/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.google.com",
      "thefoodtech.com",
      "example.com",
      "especiasmontero.com",
    ],
  },
};

export default nextConfig;
